spring:
  datasource:
    username: test
    password: test
  liquibase:
    enabled: true
    change-log: classpath:db/changelog/db.changelog-master-test.yaml
    contexts: test

  jpa:
    properties:
      hibernate:
        hbm2ddl:
          auto: validate

logging:
  level:
    root: info
    "liquibase": info
    org.springframework: warn